// @protected
/**
 * Step Definitions for: Form submission on Hyundai CPC
 * Source: CPC-allFIFOs_1.feature
 *
 * All step implementations live in BATDFIFO_auto.steps.js which fully covers this feature.
 * This file is intentionally empty to prevent duplicate step definition errors.
 *
 * Steps already handled by BATDFIFO_auto.steps.js (listed so the generator skips them):
 *   'user goes to a specific model calculator/kona'  -> regex /^user goes to a specific model (.+)$/
 *   'the user sets location postcode from test data'
 *   'user clicks on Book a test drive'
 *   'the form modal is displayed'
 *   'the user selects variant from test data and clicks Next'
 *   'the user clicks Next'
 *   'the user selects title from test data'
 *   'the user fills first name from test data'
 *   'the user fills last name from test data'
 *   'the user fills email address from test data'
 *   'the user fills phone number from test data'
 *   'the user selects purchase timeframe from test data'
 *   'the user accepts consent checkbox {int}'
 *   'the user clicks Submit request'
 *   'the BATD submission is successful'
 */
// intentionally empty
